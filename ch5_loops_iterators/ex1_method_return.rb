# What does the each method in the following program return?

#x = [1, 2, 3, 4, 5]
#x.each do |a|
#  a + 1
#end

#A: [1, 2, 3, 4, 5], doesn't actually modify the array, so it just
#   returns the original array